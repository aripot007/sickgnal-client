import { MessageData } from "Message.slint";

export struct Conversation {
    id: int,
    name: string,
    last_message: string,
    last_message_time: string,
    unread_count: int,
    is-typing: bool,
    messages: [MessageData],
}
